server:
  port: 8080

jwt:
  key_path: secret/private.key

cron:
  str: "*/5 * * * *"  # every 5 minutes. Try https://crontab.cronhub.io

database:
  url: "postgresql://root:root@database:5432/bond_picker?"